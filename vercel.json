{
  "buildCommand": "cd apps/portal && bun install && bun run build",
  "outputDirectory": "apps/portal/dist",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/:path*"
    },
    {
      "source": "/:slug*",
      "destination": "/index.html"
    }
  ]
}
